import { SubmissionProfileExplanationPipe } from './submission-profile-explanation.pipe';

describe('SubmissionProfileExplanationPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionProfileExplanationPipe();
    expect(pipe).toBeTruthy();
  });
});
